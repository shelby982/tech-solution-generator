# ── 核心框架 ──────────────────────────────────
fastapi==0.115.0
# starlette 是 fastapi 的传递依赖，但 fastapi 的约束是上界开区间。
# 不钉会被后续安装升到 1.x，届时 Router.__init__ 已无 on_startup 参数，
# 任何 APIRouter(...) 构造都会 TypeError，应用连 import 都过不去。
starlette>=0.37.2,<0.39.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# ── 文档解析 ──────────────────────────────────
pdfplumber==0.11.4
python-docx==1.1.2
Pillow>=10.0.0

# ── OCR（扫描件 + DOCX 嵌入图片兜底）────────────
# RapidOCR：PaddleOCR 同款 PP-OCRv4 模型的 ONNX 移植版，纯 Python + onnxruntime，
# 无 paddle 段错误风险，跨平台稳定。模型 ~80MB 随包分发。
rapidocr-onnxruntime==1.2.3

# ── LLM SDK ───────────────────────────────────
openai==1.51.0
anthropic==0.36.0

# ── 工具 ──────────────────────────────────────
aiofiles==24.1.0
aiosqlite==0.20.0

# ── 版本兼容性约束 ─────────────────────────────
# openai 1.51.0 向 httpx.AsyncClient 传递 proxies 参数，
# 该参数在 httpx 0.28+ 被移除，需锁定低版本。
httpx>=0.23.0,<0.28.0

# ── LangGraph 编排（Phase 4）──────────────────
langgraph>=0.2.0,<0.5.0
langgraph-checkpoint-sqlite>=2.0.0,<3.0.0
