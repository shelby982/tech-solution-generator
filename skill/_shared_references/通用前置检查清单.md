# 通用前置检查清单

适用范围：A/B/C/D 层 Skill 执行前的统一检查。

## 必查项

1. 项目路径有效：`{项目路径}` 存在且可读写。
2. 状态文件有效：`project.json`、`_state/pipeline.json` 可读取。
3. 输入资料齐备：执行当前 Skill 所需的必需文件均存在。
4. 阶段顺序正确：当前阶段允许进入目标 Skill（不跨越 A1→D2 流程）。
5. 目标对象明确：`{章节编号}` 或核验范围已明确。

## 失败处理

- 任一项不通过：立即停止当前 Skill。
- 输出缺失项清单，并给出下一步应执行的前置 Skill。
