# 通用重复执行行为

适用范围：同一章节或同一核验范围的再次执行。

## 检测逻辑

执行前检查目标输出是否已存在：

- 章节类：`20_应标草稿/章节_{章节编号}_*.md`
- 核验类：`20_应标草稿/核验报告_*.md`

## 交互选项

1. 保留已有结果并退出。
2. 基于现有结果继续优化（保留历史版本）。
3. 重新执行并生成新版本（旧文件保留，不覆盖）。

## 记录要求

- 每次重复执行都写明日期与原因。
- 保留版本差异说明，便于回溯。
